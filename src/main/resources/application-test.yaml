spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

app:
  auth:
    jwt:
      secret-base64: dGVzdC1qd3Qtc2VjcmV0LWtleS1tdXN0LWJlLWF0LWxlYXN0LTMyLWJ5dGVz
    kakao:
      client-id: test-client-id
      redirect-uris:
        - http://localhost:3000/oauth/callback/kakao
    naver:
      client-id: test-client-id
      client-secret: test-client-secret
      redirect-uris:
        - http://localhost:3000/oauth/callback/naver

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    org.hibernate.SQL: OFF
    org.hibernate.orm.jdbc.bind: OFF
